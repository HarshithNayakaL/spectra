import {serve} from "@hono/node-server";import {Hono} from "hono";import {cors} from "hono/cors";import {z} from "zod";import {runAudit} from "./pipeline";import {getAudit} from "./store";
const app=new Hono();app.use("/api/*",cors({origin:["http://localhost:5173","http://127.0.0.1:5173"]}));app.get("/api/health",c=>c.json({status:"ok",scoringVersion:"spectra-v0.1"}));app.get("/api/audits/:id",async c=>{const audit=await getAudit(c.req.param("id"));return audit?c.json(audit):c.json({error:"Audit not found"},404)});app.post("/api/audits",async c=>{const parsed=z.object({url:z.string().min(1).max(2048)}).safeParse(await c.req.json().catch(()=>null));if(!parsed.success)return c.json({error:"A URL is required"},400);const stream=new ReadableStream({async start(controller){const send=(stage:string,message:string)=>controller.enqueue(new TextEncoder().encode(JSON.stringify({type:"progress",stage,message})+"\n"));try{const audit=await runAudit(parsed.data.url,send);controller.enqueue(new TextEncoder().encode(JSON.stringify({type:"result",audit})+"\n"))}catch(e:any){controller.enqueue(new TextEncoder().encode(JSON.stringify({type:"error",message:e.message,auditId:e.auditId})+"\n"))}finally{controller.close()}}});return new Response(stream,{headers:{"content-type":"application/x-ndjson","cache-control":"no-store"}})});serve({fetch:app.fetch,port:Number(process.env.SPECTRA_API_PORT||8787)},info=>console.log(`SPECTRA API listening on ${info.port}`));
