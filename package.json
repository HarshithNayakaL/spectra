{
  "name": "spectra",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.2.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,green \"npm run dev -w @spectra/api\" \"npm run dev -w @spectra/web\"",
    "build": "bun run --cwd apps/api build && bun run --cwd apps/web build",
    "test": "bun run --cwd apps/api test && bun run --cwd packages/evaluation test && bun run --cwd packages/scoring test",
    "typecheck": "bun run --cwd apps/api typecheck && bun run --cwd apps/web typecheck && bun run --cwd packages/evaluation typecheck && bun run --cwd packages/model-gateway typecheck && bun run --cwd packages/schemas typecheck && bun run --cwd packages/scoring typecheck",
    "check": "bun run typecheck && bun run test && bun run build",
    "format": "prettier --write \"apps/**/*.{ts,tsx,css,json,mjs}\" \"packages/**/*.{ts,json}\" \"*.{json,md}\" \"docs/**/*.md\"",
    "format:check": "prettier --check \"apps/**/*.{ts,tsx,css,json,mjs}\" \"packages/**/*.{ts,json}\" \"*.{json,md}\" \"docs/**/*.md\"",
    "crawler:test": "cargo test --manifest-path services/crawler/Cargo.toml",
    "crawler:build": "cargo build --release --manifest-path services/crawler/Cargo.toml"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.9.6",
    "typescript": "^5.9.2"
  }
}
